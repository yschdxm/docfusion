可以结合，而且应该结合。但角色要分清：

- `RAG` 负责“找对内容”
- `操作计划 + 执行器` 负责“改对内容”

这两套不是替代关系，而是上下游关系。

**怎么结合最合理**
你现在那套 4 层可以扩成 5 层：

1. 自然语言理解
2. 检索增强 `RAG`
3. 结构化操作计划
4. 文档执行器
5. 结果预览/确认/落盘

也就是先检索，再规划，再执行。

**RAG 在这个项目里最有价值的地方**
不是所有场景都需要 RAG。最适合接入的有 3 类：

1. 内容查询
- 例如“这批文档里谁负责预算审批”
- 先从向量库/关键词索引里召回相关段落，再让 LLM 回答
- 这是最标准的 RAG 场景

2. 内容提取
- 例如“提取合同中的付款条件和违约责任”
- 先召回可能相关的章节，再做结构化抽取
- 比直接喂全文更稳

3. 编辑前定位
- 例如“把结论部分改得更正式”
- 先用 RAG 找到“结论部分”对应段落，再生成操作计划：
  - `rewrite_paragraph`
  - `target.paragraph_index = 18`

这里 RAG 不是直接改文档，而是帮你做“定位”。

**你这个项目里建议的结合方式**
你现在已有文档上传、抽取、知识图谱、LLM 服务，比较适合做成“两条链路”。

**链路一：问答/提取链**
适用：
- query
- extract_summary
- extract_entities
- extract_table

流程：

1. 文档解析后切块
2. 存 chunk 到向量库/检索索引
3. 用户提问
4. 先检索 top-k chunks
5. 把 chunks + 用户问题交给 LLM
6. 返回答案或结构化提取结果

这个链路里，RAG 是主角。

**链路二：编辑/排版链**
适用：
- replace_text
- rewrite_paragraph
- insert_after
- heading_promote
- list_format
- paragraph_split
- convert

流程：

1. 用户给自然语言指令
2. 先用 RAG 或规则定位相关段落/表格
3. LLM 基于“局部上下文 + 文档结构”生成操作计划 JSON
4. 执行器按 paragraph/table index 执行
5. 生成预览和输出文件

这个链路里，RAG 是辅助定位，不直接负责执行。

**一个具体例子**
用户说：

- “把文档里关于风险控制的部分改得更正式，并提取关键措施”

推荐流程：

1. `analyze_intent`
- 识别为复合意图：`edit + extract`

2. `RAG 检索`
- 找到和“风险控制”最相关的几个段落

3. `plan_document_operations`
- 输出：
```json
{
  "operations": [
    {
      "op": "rewrite_paragraph",
      "target": {"paragraph_index": 12},
      "params": {"style": "formal"}
    },
    {
      "op": "rewrite_paragraph",
      "target": {"paragraph_index": 13},
      "params": {"style": "formal"}
    },
    {
      "op": "extract_summary",
      "target": {"paragraph_indexes": [12, 13]},
      "params": {"focus": "关键措施"}
    }
  ]
}
```

4. 执行器执行改写
5. LLM基于同一批段落输出摘要
6. 返回修改预览 + 摘要结果

**为什么这比“直接全文 + LLM”更好**
因为你老师说用 RAG，本质上是在解决这几个问题：

- 文档太长，全文塞给模型成本高
- 模型容易抓错段落
- 多文档场景下上下文混乱
- 回答和编辑都缺少“证据片段”

RAG 加进来后：

- 查询更准
- 提取更稳
- 编辑定位更可控
- 可以返回“依据哪些段落做出的修改/答案”

**你项目的落地建议**
你现在最适合这样改：

1. 上传后就做 chunk + embedding
- 每个文档切成 paragraph/chunk
- 保存：
  - `document_id`
  - `chunk_id`
  - `text`
  - `paragraph_index_start/end`
  - `heading`
  - `embedding`

2. 先把 RAG 用在 `query_content`
- 这是最容易出效果的地方
- 把 [backend/app/api/v1/endpoints/agent.py](e:/fw/docfusion-main/docfusion-main/backend/app/api/v1/endpoints/agent.py) 里 `query_content` 从“全文总结”改成“检索后问答”

3. 再把 RAG 用在 `operation`
- 不是替代操作执行器
- 而是给操作计划提供目标段落

4. 最后再接知识图谱
- 图谱适合做实体关系问答和跨文档关联
- RAG 适合做段落级语义召回
- 两者可以并存：
  - 事实关系查图谱
  - 原文证据查 RAG

**你可以这样定义职责**
- `RAG`：找相关原文片段
- `KG`：找实体和关系
- `LLM`：理解意图、生成计划、总结改写
- `Executor`：真正改文件

**我建议你们答辩时这样讲**
可以把方案概括成一句话：

“我们不是把 RAG 只用于问答，而是把它前置到文档智能操作链路中，作为内容定位与证据召回模块，再结合 LLM 的操作规划能力和规则执行器，实现可解释、可控的文档编辑与信息提取。”

这句话比较工程化，也符合老师对 RAG 的期待。

如果你要，我下一步可以直接帮你把这套架构画成“项目可实现的模块图 + 调用流程图”，并且对应到你当前代码里的文件该怎么改。