QUERY_GENERATION_PROMPT = """你是一个信息检索专家。用户需要从文档库中找到填写某个表格字段所需的信息。

待填字段：{field_name}
同行上下文：{row_context}
模板表头：{table_headers}

请生成 1~3 个搜索查询，用于在文档库中找到最相关的信息。
要求：
1. 查询应简洁、关键词化，不要用完整句子
2. 如果字段是人名相关，确保查询包含人名
3. 如果字段是数值，确保查询包含相关指标名称
4. 如果有同义词，生成多个查询覆盖不同表述

输出格式（JSON）：
{{"queries": ["query1", "query2", "query3"]}}

只返回JSON，不要其他说明。"""

ANSWER_EXTRACTION_PROMPT = """你是一个精确信息提取器。根据提供的文档片段，回答用户的问题。

问题：{query}
待填字段：{field_name}

文档片段：
---
{context_text}

要求：
1. 只从提供的文档片段中提取答案，不要编造
2. 如果是数值，保留原始精度（不要四舍五入）
3. 如果是日期，保留原始格式
4. 如果是人名，确保完全一致（不要有空格/标点差异）
5. 如果找不到答案，answer 设为 null

输出格式（JSON）：
{{
  "answer": "提取的值或null",
  "confidence": 0.0~1.0,
  "source": "来自哪个片段的说明"
}}

只返回JSON，不要其他说明。"""

ROW_FILL_PROMPT = """你是一个精确数据填写助手。根据提供的模板表格和源文档片段，填写当前行中所有空字段的值。

表格结构（表头）：{table_headers}
当前行已填入的字段：{row_context}
需要填写的空字段：{empty_fields}

源文档片段：
---
{context_text}

要求：
1. 每个空字段只返回一个具体值，不要返回枚举列表、范围或描述性文字
2. 如果某字段在源文档中找不到对应值，设为 null
3. 数值保留原始精度，不要四舍五入
4. 确保同一行的所有数据来自同一条记录（同一个城市、同一个站点、同一个时间）
5. 如果已填字段中有城市名，其他空字段的值必须来自该城市的记录

输出格式（JSON）：
{{"answers": {{"字段名1": "值1或null", "字段名2": "值2或null", ...}}, "confidence": 0.0~1.0}}

只返回JSON，不要其他说明。"""

BATCH_EXTRACT_PROMPT = """你是一个精确数据提取助手。根据提供的表格结构和源文档片段，从文档中提取所有符合表格结构的数据记录。

## 文档标题（数据范围约束）
{document_title}

## 数据范围要求
- 根据文档标题，只提取与该主题直接相关的数据
- 排除与主题无关的数据

## 表格结构
表头列名（按顺序）：{table_headers}
表格说明（包含数据范围约束）：{table_context}

## 列名顺序约束
表头列的顺序非常重要。从源文档提取数据时，必须严格按照列名顺序映射数值。
例如，如果源数据行是：济南市,历下区,市监测站,138,8,36,225,68,...
对应表头：城市,区,站点名称,空气质量指数,PM10监测值,PM2.5监测值,...
则：城市=济南市, 区=历下区, 站点名称=市监测站, 空气质量指数=138, PM10监测值=8, PM2.5监测值=36, ...

## 数值提取规则
1. 仔细阅读源文档中的数据格式，确保数值对应正确的列
2. 空气质量指数、PM10监测值、PM2.5监测值等数值字段必须提取正确
3. 注意区分PM10和PM2.5的不同数值（通常PM10 > PM2.5）
4. 如果源数据中包含多个数值，按照列名顺序依次匹配
5. 数值保留原始精度，不要四舍五入

源文档片段（可能包含多种数据，你必须只提取与表格说明匹配的部分）：
---
{context_text}
---

## 提取要求
1. 只提取与数据范围匹配的数据记录
2. 每条记录的字典键必须与表头名称完全一致
3. 每个值只返回一个具体数据，不要返回枚举列表或范围
4. 必须严格按照列名顺序映射数据，确保数值对应正确的字段
5. 不要编造数据
6. 如果某列在源数据中找不到对应值，该字段设为null
7. 尽可能提取所有能找到的记录，目标数量：{max_records}条

输出格式（JSON）：
{{"records": [
  {{"表头1": "值1", "表头2": "值2", ...}},
  {{"表头1": "值3", "表头2": "值4", ...}}
]}}

只返回JSON，不要其他说明。"""

FILL_SATISFACTION_PROMPT = """你是一个数据质量评估专家。请严格评估当前填表结果是否满足要求。

## 文档标题（数据范围约束）
{document_title}

## 数据范围要求
- 根据文档标题，只提取与该主题直接相关的数据
- 排除与主题无关的数据

## 表格信息
- 表头：{table_headers}
- 总行数：{total_rows}
- 数据行数：{data_rows}
- 已填行数：{filled_rows}

## 当前填写结果（前5行示例）
{sample_rows}

## 数据来源
- 来源类型：{source_type}
- 当前查询返回数据条数：{raw_data_count}
- 说明：此数字仅为本次查询返回的数据量，不代表文档中的全部数据

## 评估维度（按优先级排序）

### 1. 数据完整性（首要）
- 评估标准：关键字段（如名称、数值、日期等标识性字段）的空值比例
- 阈值：空值比例 > 30% 视为不满足
- 检查方法：逐行检查每个关键字段是否有有效值

### 2. 数据充分性（关键）
- 评估标准：基于文档标题和已填数据，判断数据获取是否充分
- 判断方法：
  a) 检查文档标题是否暗示数据规模（如"百强"、"TOP10"、"列表"等）
  b) 如果已填数据明显少于标题暗示的规模（如标题"百强"但只有20条），视为不充分
  c) 如果数据行数看起来完整（如表格已填满或数据量与标题匹配），视为充分
  d) 如果查询返回数据达到上限（如100条）但表格仍未填满，可能还有数据
- 注意：不要仅因当前查询返回的数据量等于已填行数就判定为充分

### 3. 数据有效性
- 评估标准：数值是否在合理范围，格式是否正确
- 示例：人口不能为负数，日期格式统一，数值单位一致

### 4. 主题一致性
- 评估标准：所有数据记录是否与文档标题主题一致
- 排除：标题无关的数据、重复数据、测试数据

## 判断规则
判定为满足（is_satisfied=true）必须同时满足：
1. 关键字段空值比例 ≤ 30%（硬性要求）
2. 数据获取充分（基于标题判断，非仅比较数字）
3. 数据格式正确、数值合理

任一条件不满足 → is_satisfied=false

## 决策规则（当 is_satisfied=false 时）
- "continue"：
  - 当前查询可能未返回全部数据（如使用了LIMIT），或
  - 调整查询条件可能获取更多数据，或
  - 标题暗示应有更多数据但实际不足

- "phase_next"：
  - 当前数据源已穷尽（查询返回了所有可能数据），或
  - 当前数据源无法提供缺失的关键字段

- "document_next"：
  - 当前文档的主题与表格需求不匹配

## 输出格式（JSON）
{{
  "is_satisfied": true/false,
  "reason": "具体说明各评估维度的检查结果和判断依据",
  "missing_fields": ["未填写或空值过多的字段名"],
  "decision": "continue/phase_next/document_next",
  "suggestions": "具体改进建议"
}}

只返回JSON，不要其他说明。"""
